int i,n,a=0,b=0;
if (strlen(s1) > strlen(s2)) n = strlen(s1);
else n = strlen(s2);

for (i=0;i<n;i++){
    a += *(s1+i);
    b += *(s2+i);
}
if (a>b) return 1;
else if (a==b) return 0;
else return -1;
